# services

Lớp gọi API/dịch vụ bên ngoài dùng chung toàn app (ví dụ: HTTP client, cấu hình auth provider).

Service riêng của một feature (ví dụ gọi API khoá học) đặt trong `features/<feature-name>/services`, không đặt ở đây.
