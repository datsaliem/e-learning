# types

Type/interface dùng chung toàn app (ví dụ: kiểu dữ liệu response API chung, enum trạng thái chung).

Type riêng của một feature (ví dụ `Course`, `Lesson`) đặt trong `features/<feature-name>/types.ts`, không đặt ở đây.
