# components/shared

Component dùng chung, không phụ thuộc feature cụ thể (ví dụ: `EmptyState`, `PageHeader`, `LoadingSpinner`).

Component thuần UI (button, input, dialog...) từ shadcn/ui nằm ở `components/ui`, không đặt ở đây.
